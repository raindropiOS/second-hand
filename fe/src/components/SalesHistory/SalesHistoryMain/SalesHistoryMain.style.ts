import styled from 'styled-components';

const $SalesHistoryMainContainer = styled.main`
  margin-top: 52px;
  // FIXME(jayden): !important 수정하기
  height: calc(100vh - 185px) !important;
`;

export { $SalesHistoryMainContainer };
